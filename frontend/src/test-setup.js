// Global Vitest setup file
// Imports @testing-library/jest-dom matchers so every test file
// can use .toBeInTheDocument(), .toHaveValue(), etc. without explicit imports.
import '@testing-library/jest-dom';
